<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ounce.bops4zombies">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/zombieslogo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TextActivity"></activity>
        <activity android:name=".easterEgg.EEBloodOfTheDead" />
        <activity android:name=".Images" />
        <activity android:name=".maps.BloodOfTheDead" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>